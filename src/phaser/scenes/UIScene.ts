import Phaser from 'phaser';

export class UIScene extends Phaser.Scene {
  constructor() {
    super({ key: 'UIScene' });
  }

  create() {
    // A cena de UI do Phaser está pronta para receber elementos de menu, HUD, etc.
  }
}
